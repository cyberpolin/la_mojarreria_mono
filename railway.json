{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bash -lc 'if [ -f ./apps/api/package.json ]; then npm --prefix ./apps/api run build; else npm run build; fi'"
  },
  "deploy": {
    "startCommand": "bash -lc 'if [ -f ./apps/api/package.json ]; then npm --prefix ./apps/api run start; else npm run start; fi'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
