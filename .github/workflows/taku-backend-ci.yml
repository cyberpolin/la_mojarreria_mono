name: TAKU Backend CI

on:
  pull_request:
    paths:
      - "taku-backend/**"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "package.json"
      - ".github/workflows/taku-backend-ci.yml"
  push:
    branches:
      - main
      - taku-deployment
    paths:
      - "taku-backend/**"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "package.json"
      - ".github/workflows/taku-backend-ci.yml"

jobs:
  typecheck-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check service changes
        id: changes
        env:
          CHECK_BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          CHECK_HEAD_SHA: ${{ github.sha }}
        run: scripts/check-service-changes.sh taku-backend

      - name: Setup pnpm
        if: steps.changes.outputs.changed == 'true'
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.5

      - name: Setup Node
        if: steps.changes.outputs.changed == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 20.19.4

      - name: Install dependencies
        if: steps.changes.outputs.changed == 'true'
        run: pnpm install --no-frozen-lockfile

      - name: Typecheck
        if: steps.changes.outputs.changed == 'true'
        run: pnpm --filter @taku/backend typecheck

      - name: Build
        if: steps.changes.outputs.changed == 'true'
        run: pnpm --filter @taku/backend build
