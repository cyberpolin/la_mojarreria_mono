[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @mojarreria/api build"

[deploy]
startCommand = "pnpm --filter @mojarreria/api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
