{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm run build && echo 'Build completed! from apps/api'"
  },
  "deploy": {
    "preDeployCommand": "npm run migrate:prod && npm run seed:prod && echo 'After build command executed!'",
    "startCommand": "npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "startCommand": "npm run migrate:prod && npm run seed:prod && npm run start"
}
